let mode='listen',history=[],sound=localStorage.naviSound!=='off',audioCtx;
const titles={listen:'NAVI hört dir zu',sort:'Wir sortieren gemeinsam',calm:'NAVI bleibt bei dir',plan:'Ein kleiner nächster Schritt'};
const starters={listen:'Du kannst einfach anfangen. Was liegt dir gerade auf dem Herzen?',sort:'Wir müssen nicht alles gleichzeitig lösen. Was ist gerade der lauteste Gedanke?',calm:'Ich bin hier. Spür einmal den Boden unter deinen Füßen – was macht dein Körper gerade?',plan:'Okay, wir suchen nur einen machbaren Schritt. Worum geht es gerade?'};
const $=s=>document.querySelector(s), messages=$('#messages'),input=$('#input'),orb=$('#orb');
function goHome(){$('#chat').classList.add('hidden');$('#home').classList.remove('hidden');history=[]}
function openChat(next){mode=next;history=[];$('#home').classList.add('hidden');$('#chat').classList.remove('hidden');$('#modeTitle').textContent=titles[mode];messages.innerHTML='';$('#crisis').classList.add('hidden');addMessage('navi',starters[mode]);input.focus()}
function addMessage(role,text,typing=false){const div=document.createElement('div');div.className='msg '+role+(typing?' typing':'');div.textContent=text;messages.appendChild(div);messages.scrollTop=messages.scrollHeight;return div}
$('#form').addEventListener('submit',async e=>{e.preventDefault();const text=input.value.trim();if(!text)return;input.value='';addMessage('user',text);const pending=addMessage('navi','NAVI denkt',true);orb.classList.add('thinking');try{const res=await fetch('/api/chat',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({message:text,mode,history})});const data=await res.json();pending.remove();if(!res.ok)throw new Error(data.error||'Keine Verbindung');addMessage('navi',data.reply);history.push({role:'user',content:text},{role:'assistant',content:data.reply});history=history.slice(-10);if(data.crisis)$('#crisis').classList.remove('hidden');speak(data.reply)}catch(err){pending.remove();addMessage('navi',err.message+' Du kannst es in einem Moment noch einmal versuchen.')}finally{orb.classList.remove('thinking')}});
function clearChat(){history=[];messages.innerHTML='';$('#crisis').classList.add('hidden');addMessage('navi',starters[mode])}
function toggleSound(){sound=!sound;localStorage.naviSound=sound?'on':'off';$('#sound').textContent=sound?'♫':'×';tone()}
function tone(){if(!sound)return;audioCtx=audioCtx||new(window.AudioContext||window.webkitAudioContext)();const o=audioCtx.createOscillator(),g=audioCtx.createGain();o.frequency.value=520;g.gain.setValueAtTime(.035,audioCtx.currentTime);g.gain.exponentialRampToValueAtTime(.001,audioCtx.currentTime+.2);o.connect(g);g.connect(audioCtx.destination);o.start();o.stop(audioCtx.currentTime+.2)}
function speak(text){tone();orb.classList.add('talking');setTimeout(()=>orb.classList.remove('talking'),Math.min(2600,500+text.length*12))}
function startVoice(){const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SR){addMessage('navi','Spracheingabe wird von diesem Browser leider noch nicht unterstützt.');return}const r=new SR();r.lang='de-DE';r.interimResults=false;$('#mic').classList.add('listening');r.onresult=e=>{input.value=e.results[0][0].transcript};r.onend=()=>$('#mic').classList.remove('listening');r.onerror=()=>$('#mic').classList.remove('listening');r.start()}
document.addEventListener('pointermove',e=>{const f=$('#face'),r=orb.getBoundingClientRect(),x=Math.max(-6,Math.min(6,(e.clientX-r.left-r.width/2)/28)),y=Math.max(-5,Math.min(5,(e.clientY-r.top-r.height/2)/28));f.style.transform=`translate(${x}px,${y}px)`});
